control:
  action:

    launch_terminal: |
      launch CONFIG['prefer']['terminal']

    launch_browser: |
      launch CONFIG['prefer']['browser']

    launch_filer: |
      launch CONFIG['prefer']['filer']

    launch_editor: |
      launch CONFIG['prefer']['editor']

    launch_music: |
      launch CONFIG['prefer']['music']
